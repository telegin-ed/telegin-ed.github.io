title: telegin-ed
description: Summary и статьи
lang: ru
theme: jekyll-theme-primer
sass:
  quiet_deps: true
  silence_deprecations: ["import"]
plugins:
  - jekyll-seo-tag
  - jekyll-relative-links
defaults:
  - scope:
      path: ""
    values:
      layout: default